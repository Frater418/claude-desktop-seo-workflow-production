import { useState } from "react"
import type { ActionIntent, ActionPayload, ArtifactRecord } from "../generated/api-types"
import type { OperatorApiClient } from "../api/client"
import type { GateRead } from "../api/readModels"
import { gateStatusLabel } from "../api/statusLabels"
import { RouteActionFooter } from "./RouteActionFooter"
import type { OperatorWorkspaceData } from "./useOperatorWorkspace"
import { Step4ReviewPayload } from "./Step4ReviewPayload"
import { useAdminAction } from "./useAdminAction"
import { useReviewArtifactPayload } from "./useReviewArtifactPayload"

type ReviewAction = Extract<ActionIntent["action"], "approve" | "reject" | "request-revision" | "request-input" | "escalate" | "request-waiver">
type ReviewFields = { readonly reason: string; readonly instructions: string; readonly affectedSections: string; readonly immutableConstraints: string; readonly options: string; readonly impacts: string }
type ReviewWorkspaceProps = { readonly api: OperatorApiClient; readonly data: OperatorWorkspaceData; readonly onReadback: () => Promise<void> }
const emptyFields: ReviewFields = { reason: "", instructions: "", affectedSections: "", immutableConstraints: "", options: "", impacts: "" }
const actionConfig: Readonly<Record<ReviewAction, { readonly label: string; readonly confirmation: string; readonly preview: string }>> = {
  approve: { label: "Freigabe", confirmation: "Freigabe bestaetigen", preview: "Freigabe vorbereiten" }, reject: { label: "Ablehnung", confirmation: "Ablehnung bestaetigen", preview: "Vorschau fuer Ablehnung erstellen" }, "request-revision": { label: "Revision", confirmation: "Revision anfordern bestaetigen", preview: "Vorschau fuer Revision erstellen" }, "request-input": { label: "Eingabe", confirmation: "Eingabe anfordern bestaetigen", preview: "Vorschau fuer Eingabe erstellen" }, escalate: { label: "Eskalation", confirmation: "Eskalation bestaetigen", preview: "Vorschau fuer Eskalation erstellen" }, "request-waiver": { label: "Ausnahme", confirmation: "Ausnahmeanfrage bestaetigen", preview: "Vorschau fuer Ausnahme erstellen" },
}
function lines(value: string): readonly string[] { return value.split("\n").map((entry) => entry.trim()).filter((entry) => entry !== "") }
function payload(action: ReviewAction, fields: ReviewFields): ActionPayload | undefined {
  switch (action) { case "approve": return undefined; case "reject": case "request-revision": return { reason: fields.reason, instructions: fields.instructions, affected_sections: lines(fields.affectedSections), immutable_constraints: lines(fields.immutableConstraints) }; case "request-input": case "request-waiver": return { reason: fields.reason, instructions: fields.instructions }; case "escalate": return { reason: fields.reason, options: lines(fields.options), impacts: lines(fields.impacts) } }
}
function fieldsComplete(action: ReviewAction, fields: ReviewFields): boolean {
  switch (action) { case "approve": return true; case "reject": case "request-revision": return fields.reason !== "" && fields.instructions !== "" && lines(fields.affectedSections).length > 0 && lines(fields.immutableConstraints).length > 0; case "request-input": case "request-waiver": return fields.reason !== "" && fields.instructions !== ""; case "escalate": return fields.reason !== "" && lines(fields.options).length >= 2 && lines(fields.impacts).length > 0 }
}
function reviewAction(value: string): ReviewAction {
  switch (value) { case "approve": return value; case "reject": return value; case "request-revision": return value; case "request-input": return value; case "escalate": return value; case "request-waiver": return value; default: return "approve" }
}
function matchingGate(data: OperatorWorkspaceData, artifact: ArtifactRecord | null): GateRead | undefined { return artifact === null ? undefined : data.gates.find((gate) => gate.tenantId === data.currentRun.tenant_id && gate.projectId === data.currentRun.project_id && gate.runId === data.currentRun.run_id && gate.stepId === data.currentRun.step_id && gate.artifactId === artifact.artifact_id && gate.artifactHash === artifact.content_sha256 && gate.artifactRevision === artifact.revision) }

export function ReviewWorkspace({ api, data, onReadback }: ReviewWorkspaceProps): JSX.Element {
  const [selectedAction, setSelectedAction] = useState<ReviewAction>("approve")
  const [fields, setFields] = useState<ReviewFields>(emptyFields)
  const action = useAdminAction({ client: api, reload: onReadback })
  const artifact = data.current.artifact
  const gate = matchingGate(data, artifact)
  const payloadState = useReviewArtifactPayload({ api, data })
  const exactEvidence = artifact !== null && gate !== undefined && payloadState.kind === "ready"
  const locked = action.state.kind === "awaiting-confirmation" || action.state.kind === "confirming" || action.state.kind === "reloading"
  const prepare = (reviewAction: ReviewAction): void => { setSelectedAction(reviewAction); const actionPayload = payload(reviewAction, fields); const intent = actionPayload === undefined ? { action: reviewAction, tenant_id: data.currentRun.tenant_id, project_id: data.currentRun.project_id, run_id: data.currentRun.run_id, step_id: data.currentRun.step_id, expected_revision: data.currentRun.expected_revision } : { action: reviewAction, tenant_id: data.currentRun.tenant_id, project_id: data.currentRun.project_id, run_id: data.currentRun.run_id, step_id: data.currentRun.step_id, expected_revision: data.currentRun.expected_revision, payload: actionPayload }; void action.preview(intent) }
  const selectAction = (reviewAction: ReviewAction): void => { setSelectedAction(reviewAction); setFields(emptyFields) }
  const update = (field: keyof ReviewFields, value: string): void => setFields({ ...fields, [field]: value })
  const result = action.state.kind === "awaiting-confirmation" ? action.state.preview.consequence["result"] : ""
  return <section className="review-layout"><section className="work-panel"><p className="eyebrow">Pruefungen und Freigaben</p><h2>Entscheidung vorbereiten</h2>{artifact === null || gate === undefined ? <section className="action-blocker"><h3>Keine exakt gebundene Pruefung</h3><p>Eine Freigabe setzt ein kanonisches Artefakt und einen dazu passenden Pruefnachweis voraus.</p><p>Aktuelles Artefakt und Pruefnachweis erneut laden oder die Pruefung fuer diese Revision abschliessen.</p></section> : <section className="gate-report"><h3>{artifact.storage_key.split("/").at(-1) ?? artifact.storage_key}, Revision {artifact.revision}</h3><dl className="facts"><div><dt>Gate-Ergebnis</dt><dd>{gateStatusLabel(gate.result)}</dd></div><div><dt>Nachweis</dt><dd>{Object.entries(gate.evidence).map(([name, value]) => <span key={name}>{name}: {String(value)} </span>)}</dd></div><div><dt>Feststellungen</dt><dd>{gate.findings.length === 0 ? "Keine Feststellungen" : gate.findings.join("; ")}</dd></div><div><dt>Pruefer</dt><dd>{gate.checkerVersion}</dd></div><div><dt>Geprueft am</dt><dd>{gate.checkedAt}</dd></div></dl></section>}<Step4ReviewPayload gate={gate} state={payloadState} />{selectedAction === "approve" ? null : <section className="action-consequence"><h3>{actionConfig[selectedAction].label} begruenden</h3><label>Begruendung<textarea aria-label="Begruendung" value={fields.reason} onChange={(event) => update("reason", event.currentTarget.value)} disabled={locked} /></label>{selectedAction === "reject" || selectedAction === "request-revision" || selectedAction === "request-input" || selectedAction === "request-waiver" ? <label>{selectedAction === "request-waiver" ? "Pruefanweisung fuer Ausnahme" : "Anweisungen"}<textarea aria-label={selectedAction === "request-waiver" ? "Pruefanweisung fuer Ausnahme" : "Anweisungen"} value={fields.instructions} onChange={(event) => update("instructions", event.currentTarget.value)} disabled={locked} /></label> : null}{selectedAction === "reject" || selectedAction === "request-revision" ? <><label>Betroffene Abschnitte<textarea aria-label="Betroffene Abschnitte" value={fields.affectedSections} onChange={(event) => update("affectedSections", event.currentTarget.value)} disabled={locked} /></label><label>Unveraenderliche Vorgaben<textarea aria-label="Unveraenderliche Vorgaben" value={fields.immutableConstraints} onChange={(event) => update("immutableConstraints", event.currentTarget.value)} disabled={locked} /></label></> : null}{selectedAction === "escalate" ? <><label>Mindestens zwei Optionen<textarea aria-label="Mindestens zwei Optionen" value={fields.options} onChange={(event) => update("options", event.currentTarget.value)} disabled={locked} /></label><label>Auswirkungen<textarea aria-label="Auswirkungen" value={fields.impacts} onChange={(event) => update("impacts", event.currentTarget.value)} disabled={locked} /></label></> : null}</section>}<section aria-live="polite">{action.state.kind === "idle" && action.state.notice !== undefined ? <p className="action-blocker">{action.state.notice}</p> : null}{action.state.kind === "blocked" ? <section className="action-blocker"><h3>Aktion nicht erlaubt</h3>{action.state.preview.blockers.map((blocker) => <div key={blocker.code}><p>{blocker.message}</p><p>{blocker.remediation}</p></div>)}</section> : null}{action.state.kind === "awaiting-confirmation" ? <section className="action-consequence"><h3>Konkrete Folge</h3><p>{typeof result === "string" ? result : "Folge der Aktion wurde vorbereitet."}</p></section> : null}{action.state.kind === "reloading" ? <p>Kanonischer Stand wird geladen.</p> : null}{action.state.kind === "completed" ? <p className="success-note">{action.state.replay ? "Kanonische Wiederholung bestaetigt." : "Kanonischer Stand aktualisiert"}</p> : null}{action.state.kind === "failed" ? <section className="action-blocker"><h3>Aktion fehlgeschlagen</h3><p>{action.state.message}</p></section> : null}</section></section><aside className="evidence-inline"><h3>Pruefnachweis</h3><p>Freigaben werden erst nach Vorschau, Bestaetigung und kanonischem Readback angezeigt.</p></aside><RouteActionFooter><section className="persistent-actions" aria-label="Pruefungsaktionen"><div><p className="eyebrow">Pruefung</p><h3>Entscheidung ausfuehren</h3></div><label>Entscheidung waehlen<select aria-label="Entscheidung waehlen" value={selectedAction} onChange={(event) => selectAction(reviewAction(event.currentTarget.value))} disabled={locked}>{Object.entries(actionConfig).map(([value, config]) => <option key={value} value={value}>{config.label}</option>)}</select></label><button type="button" onClick={() => prepare(selectedAction)} disabled={locked || !fieldsComplete(selectedAction, fields) || (selectedAction === "approve" && !exactEvidence)}>{actionConfig[selectedAction].preview}</button>{action.state.kind === "awaiting-confirmation" ? <button type="button" onClick={() => { void action.confirm() }}>{actionConfig[selectedAction].confirmation}</button> : null}</section></RouteActionFooter></section>
}
