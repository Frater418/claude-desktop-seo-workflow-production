"""Heartweb runtime-neutral domain contract package.

Autor: Raphael Rechberger

Import concrete APIs from ``services.domain_contract.validator`` so the
module can also run cleanly through ``python -m``.
"""
